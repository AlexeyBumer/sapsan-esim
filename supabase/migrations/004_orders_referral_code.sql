-- ============================================================
-- Sapsan eSIM — миграция: реферальный код при оформлении заказа
-- Вставьте в Supabase → SQL Editor → Run.
-- ============================================================

-- Сохраняем код, введённый при оформлении заказа (в т.ч. гостем).
-- Используется на следующем этапе при наполнении реферального баланса.
alter table public.orders
  add column if not exists referral_code text;
